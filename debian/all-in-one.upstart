# educloud daemon service

description "eduCloud daemon"
author "Thomas Li<thomas.li@educloud.com>"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn

script
  if [ -e /etc/default/all-in-one ]; then
    . /etc/default/all-in-one
  fi
  exec start-stop-daemon --start --chuid luhya --exec /usr/local/bin/all-in-one
end script
