# educloud daemon service

description "eduCloud daemon"
author "Thomas Li<thomas.li@educloud.com>"


start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn

script
  if [ -e /etc/default/nodedaemon-cc ]; then
    . /etc/default/nodedaemon-cc
  fi
  exec start-stop-daemon --start --chuid luhya --exec /usr/local/bin/nodedaemon-cc
end script
