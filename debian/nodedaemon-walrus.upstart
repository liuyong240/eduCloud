# educloud daemon service

description "eduCloud daemon"
author "Thomas Li<thomas.li@educloud.com>"


start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn

script
  if [ -e /etc/default/nodedaemon-walrus ]; then
    . /etc/default/nodedaemon-walrus
  fi
  exec start-stop-daemon --start --chuid luhya --exec /usr/local/bin/nodedaemon-walrus
end script
