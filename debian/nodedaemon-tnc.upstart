# educloud daemon service

description "eduCloud daemon"
author "Thomas Li<thomas.li@educloud.com>"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn

script
  if [ -e /etc/default/nodedaemon-tnc ]; then
    . /etc/default/nodedaemon-tnc
  fi
  exec start-stop-daemon --start --chuid $USER --exec /usr/local/bin/nodedaemon-tnc
end script
