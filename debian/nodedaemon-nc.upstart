# luhya-server

description "luhya server Agent"
author "Thomas Li<thomas.li@sinobot.com.cn>"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn

script
  if [ -e /etc/default/luhya-server ]; then
    . /etc/default/luhya-server
  fi
  exec start-stop-daemon --start --chuid luhya --exec /usr/local/bin/luhya-server
end script
